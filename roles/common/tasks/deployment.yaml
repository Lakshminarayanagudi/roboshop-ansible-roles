- name: "strop {{component}}"
  tags:
  - deployment
  ansible.builtin.service:
    name: "{{component}}"
    state: stopped

- name: Recursively remove app directeory
  tags:
  - deployment
  ansible.builtin.file:
    path: "/app/*"
    state: absent