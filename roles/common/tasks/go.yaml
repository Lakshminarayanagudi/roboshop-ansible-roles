- name: install go
  ansible.builtin.dnf:
    name: golang
    state: present

- name: build go aplication
  ansible.builtin.command: "{{item}}"
  loop:
  - go mod init dispatch
  - go get
  - go buld
  args:
    chdir: /app